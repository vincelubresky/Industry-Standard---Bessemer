# Copy this file's variables into your Render Web Service's Environment settings.
# Do NOT commit a real .env file with real credentials.

# Postgres connection string (from Render Postgres, Supabase, etc.)
DATABASE_URL=postgres://user:password@host:5432/dbname

# Render sets PORT automatically; only needed for local testing.
PORT=3000
